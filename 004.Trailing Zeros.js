/**
 * 004.Trailing Zeros-2
 * 
 * Write an algorithm which computes the number of trailing zeros in n factorial.
 * 
 * Example:
 * 11! = 39916800, so the out should be 2
 */
var n = 39916800;

function trailingZeros(n) {
    var strN = n.toString();
    console.log(strN);
};